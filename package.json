{
  "name": "array-transforms",
  "version": "1.0.0",
  "main": "dist/index.ts",
  "repository": "https://github.com/kahmannf/array-transforms.git",
  "author": "Felix Kahmann <felix@kahmann.de>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build-and-publish": "tsc && node fix-carriage-returns dist && cd dist && npm publish --access public",
    "test": "jest",
    "coverage": "jest --coverage"
  },
  "devDependencies": {
    "@types/jest": "^24.0.18",
    "jest": "^24.9.0",
    "ts-jest": "^24.1.0",
    "typescript": "^3.6.3"
  }
}
